specVersion: "4.0"
metadata:
  name: ui5.community.jsx.runtime
type: library
framework:
  name: OpenUI5
  version: "1.148.2"
  libraries:
    - name: sap.ui.core
    - name: themelib_sap_horizon
    - name: sap.m
      development: true
customConfiguration:
  config-ui5-tooling-transpile: &cfgTranspile
    debug: true
    generateDts: true
    generateTsInterfaces: true
    omitTSFromBuildResult: true
    # Transform the JSX/TSX syntax via `@babel/plugin-transform-react-jsx`
    # (an optional peer dep the tooling resolves). Replaces the former
    # hand-authored `.babelrc.json`; the TypeScript/UI5/env presets are
    # assembled by the tooling from `tsconfig.json` + browserslist. The
    # only JSX in this library is the `<Switch>` sample plugin.
    transformJSX:
      runtime: automatic
      importSource: "ui5/community/jsx/runtime"
  config-ui5-tooling-modules: &cfgModules
    debug: true
builder:
  customTasks:
    - name: ui5-tooling-transpile-task
      afterTask: replaceVersion
      configuration: *cfgTranspile
    - name: ui5-tooling-modules-task
      afterTask: ui5-tooling-transpile-task
      configuration: *cfgModules
server:
  customMiddleware:
    - name: ui5-tooling-transpile-middleware
      afterMiddleware: compression
      configuration: *cfgTranspile
    - name: ui5-tooling-modules-middleware
      afterMiddleware: ui5-tooling-transpile-middleware
      configuration: *cfgModules
    - name: ui5-middleware-livereload
      afterMiddleware: compression
